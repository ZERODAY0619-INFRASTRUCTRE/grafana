apiVersion: 1

providers:
  - name: Caddy Proxy Manager
    orgId: 1
    folder: Caddy Proxy Manager
    type: file
    disableDeletion: true
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
      foldersFromFilesStructure: false
